import styled from 'styled-components';

const FormContainer = styled.form`
  display: flex;
  flex-direction: column;
  justify-content: center;

  width: 360px;
`;

const RadioBtnContainer = styled.div`
  display: flex;
`;

export { FormContainer, RadioBtnContainer };
